streamlit>=1.36.0
requests>=2.31.0
beautifulsoup4>=4.12.3
trafilatura>=1.9.0
pdfplumber>=0.11.0
python-docx>=1.1.0
langdetect>=1.0.9
python-dotenv>=1.0.1
groq>=0.9.0
pytesseract>=0.3.10
Pillow>=10.0.0
PyMuPDF>=1.23.0

# Optional — enables headless-browser rendering as a fallback for
# JavaScript-heavy sites (React/Next.js/Vue apps). Not required for most
# sites; the app works fine without it, just with a warning on JS-heavy pages.
# After installing, run once: playwright install chromium
# playwright>=1.45.0